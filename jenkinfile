pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Building the Dev Branch"
            }
        }
        stage('Test') {
            steps {
                echo "Testing the Dev Branch"
            }
        }
    }
}
